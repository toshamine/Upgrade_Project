{% extends 'base.html.twig' %}

{% block title %}RDV{% endblock %}

{% block body %}
    <h1>RDV</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ r_d_v.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ r_d_v.Date ? r_d_v.Date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ r_d_v.Status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('r_d_v_index') }}">back to list</a>

    <a href="{{ path('r_d_v_edit', {'id': r_d_v.id}) }}">edit</a>

    {{ include('rdv/_delete_form.html.twig') }}
{% endblock %}
