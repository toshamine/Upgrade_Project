<form method="post" action="{{ path('white_test_delete', {'id': white_test.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ white_test.id) }}">
    <button class="btn">Delete</button>
</form>
