<!DOCTYPE html>

<title>Edit WhiteTest</title>

{% block body %}
    <h1>Edit WhiteTest</h1>

    {{ include('white_test/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('white_test_index') }}">back to list</a>

    {{ include('white_test/_delete_form.html.twig') }}
{% endblock %}
